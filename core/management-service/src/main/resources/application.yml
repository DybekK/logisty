quarkus:
  datasource:
    db-kind: postgresql
    username: postgres
    password: postgres
    reactive:
      url: vertx-reactive:postgresql://localhost:5432/logisty
    jdbc:
      url: jdbc:postgresql://localhost:5432/logisty
  flyway:
    migrate-at-start: true